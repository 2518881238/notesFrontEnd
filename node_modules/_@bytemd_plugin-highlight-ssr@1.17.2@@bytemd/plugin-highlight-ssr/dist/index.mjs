import rehypeHighlight from "rehype-highlight";
function highlightSsr({
  subset = false,
  ignoreMissing = true,
  ...rest
} = {}) {
  return {
    rehype: (processor) => processor.use(rehypeHighlight, { subset, ignoreMissing, ...rest })
  };
}
export {
  highlightSsr as default
};
