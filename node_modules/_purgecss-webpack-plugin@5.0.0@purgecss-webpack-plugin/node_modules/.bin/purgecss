#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_purgecss@5.0.0@purgecss/bin/purgecss.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_purgecss@5.0.0@purgecss/bin/purgecss.js" "$@"
  ret=$?
fi
exit $ret
